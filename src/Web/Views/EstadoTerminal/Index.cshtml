@model IEnumerable<BusinessEntity.EstadoTerminal>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table>
    <tr>
        <th>
            Nombre
        </th>
        <th>
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.ActionLink("Editar", "editar", new { id = item.Id }) |
                @using (Html.BeginForm("eliminar","estadoterminal",new { id = item.Id }))
                {
                    <input type="submit" value="Eliminar" onclick="return confirm('¿Estas seguro?');" />
                }
            </td>
        </tr>
    }
</table>

<p>
    @Html.ActionLink("Crear", "crear")
</p>